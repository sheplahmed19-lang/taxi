# Ride-Hailing Platform — Project Instructions

## What this is
Uber-style ride-hailing platform: Flutter rider & driver apps, Express+TS API,
Postgres+PostGIS, Redis, Socket.IO realtime, React admin panels, FCM push.

## Commands
- API dev:        pnpm --filter api dev
- API tests:      pnpm --filter api test
- Migrations:     pnpm --filter api migrate:dev / migrate:deploy
- Web dev:        pnpm --filter web dev
- Rider app:      cd mobile/rider_app && flutter run
- Driver app:     cd mobile/driver_app && flutter run
- Full stack:     docker compose up -d (postgres, redis) then API dev
- Lint all:       pnpm lint && (cd mobile/rider_app && flutter analyze)

## Architecture rules (do not violate)
1. Modular monolith. Modules under apps/api/src/modules/. A module exposes
   routes + service; other modules import only its service, never its internals.
2. Trip state machine transitions happen ONLY in trips/service via
   transitionTrip(tripId, event). Valid transitions are defined in
   trips/state-machine.ts. Apps never set status directly.
3. ALL wallet/balance changes go through wallet/ledger.ts postEntry() inside a
   DB transaction with row locks. Never UPDATE a balance column directly.
4. Fare calculation is the pure function in fares/engine.ts. It returns an
   itemized breakdown; the full breakdown JSON is stored on the trip.
5. Payment providers implement the PaymentGateway interface in
   payments/gateway.interface.ts. Webhooks must be signature-verified.
6. Geo queries use PostGIS (zones, geofences) and Redis GEO (live driver
   positions). Driver live location is NEVER read from Postgres.
7. All request bodies validated with zod schemas colocated in each module.
8. API is versioned under /api/v1. Responses: { success, data | error }.
9. Socket.IO events and payloads are defined in docs/socket-events.md.
   Update that file whenever an event changes.
10. Config values (fares, commission %, dispatch radius, OTP length, timeouts)
    are read from system_config table via config service — never hardcoded.

## Code style
- TypeScript strict mode everywhere. No `any` in money or trip code.
- Money stored as integer minor units (piastres/cents). Use shared/money.ts.
- Flutter: riverpod for state, dio for HTTP, socket_io_client for realtime,
  freezed models mirroring API DTOs. Feature-first folder structure.
- Tests required for: fares/engine.ts, wallet/ledger.ts, trips/state-machine.ts,
  dispatch ranking logic, promo validation.

## Current phase
Update this line as you progress: **Phase 1 done (1.1–1.8; the acceptance milestone itself needs a real device/emulator with two phones, unavailable in this sandbox). Phase 2 — Money Layer — done in full (2.1–2.5): wallet/ledger hardening; Stripe + Paystack gateways; wallet ride-payment with insufficient-balance cash fallback; per-vehicle-type commission override + subscription mode (100% earnings while active, wallet/gateway purchase, supersede-not-stack, BullMQ expiry job); and 2.5 — payouts (request reserves the amount out of the wallet immediately via postEntry, not at approval time, so nothing lets a driver over-request against pending payouts; approve/reject/mark-paid workflow, reject refunds the reservation) + owe management (driver "pay owe from wallet" self-service, admin report + manual delta adjustment, both new admin actions logged to a new AuditLog model) + weekly driver CSV statements (BullMQ Monday-00:00 cron, `jobs/weeklyStatements.ts`; upload step is injectable so the aggregation logic is fully tested without a real S3 endpoint — no MinIO running in this sandbox, so the upload itself is unverified end-to-end, same disclosed category as GOOGLE_MAPS_API_KEY/STRIPE_SECRET_KEY). Also fixed a second instance of the same migration hazard from 2.4: `prisma migrate dev` went non-interactive-refusal on this migration, so the SQL was generated via `prisma migrate diff`, hand-stripped of the same spurious GiST-index DROP statements, applied directly, and registered via `prisma migrate resolve --applied`; verified via a from-scratch `migrate deploy` that all 9 migrations apply cleanly with the indexes intact. Phase 3 — Growth Features — 3.1 promo codes done (backend; no schema migration needed, PromoCode/PromoRedemption/Trip.promoId already existed from Phase 0's schema): `promos/service.ts` centralizes eligibility (active window, min fare, vehicle-type/zone allow-lists, overall + per-user usage limits) behind `resolveAndValidatePromo`, reused by both the no-side-effect `POST /promos/validate` preview and the two real attach points — `POST /trips` with an optional `promoCode` (validated against that same request's fare estimate before the trip row is even created) and `POST /promos/apply` for adding a code to an already-requested trip before it starts. A `PromoRedemption` row is written the moment a promo actually attaches to a trip (that's what usage limits count against), and `cancelTrip` deletes it again so a cancelled ride never burns the slot. `fares/engine.ts`'s discount math was extracted into a standalone `applyPromoDiscount` so `completeTrip` can re-apply a trip's already-attached promo's raw terms against the newly-measured final fare — locked in, without re-checking eligibility — matching the "lock-at-completion" plan requirement. Full admin CRUD lives under `/admin/promos` (delete blocked once a promo has redemptions — deactivate instead). Rider-app promo entry UI is deferred (backend-only pass, matching the project's usual backend-then-app cadence within a sub-phase). Phase 3.2 — Referrals done (backend; no schema migration needed either, User.referralCode/referredBy + Referral model already existed from Phase 0's schema): every user gets a unique 8-char referral code generated at account creation (`referrals/service.ts:generateReferralCode`, `auth/service.ts:findOrCreateUserByPhone`); an optional `referralCode` on `POST /auth/otp/verify` links a brand-new user to their referrer (silently ignored — never a signup blocker — on an invalid or self code, and only on the new-account path, never on a returning login). A BullMQ job (`jobs/referralBonus.ts`), enqueued the moment either trip participant's trip settles to "paid" (`trips/service.ts`'s `completeTrip`/`markTripPaid`), checks whether that specific user has a pending referral and credits both referrer and referee via the wallet ledger from `system_config`'s `referral_bonus_rider`/`referral_bonus_driver` — this is Phase 3's "credit both after referee's first completed trip" requirement, satisfied for free by only ever firing while `bonusStatus` is still `pending`. Ledger entries are posted *before* the referral flips to `credited` (not after) so a crash mid-job can never leave a referral marked paid without the money having moved, and `postEntry`'s idempotency key makes a retried or racing duplicate call a safe no-op rather than a double-credit — caught and fixed during this pass after a first implementation flipped the status first and briefly raced a test. `GET /referrals/my-code` and `/stats` round out the module. Rider/driver app referral share-sheet and dashboard UI deferred, same backend-then-app cadence. Phase 3.3 — Scheduled rides done (backend; DID need a migration this time — `ScheduledRide` existed from Phase 0 with only free-text pickup/drop addresses and a bare-string `status`, no coordinates to actually create a trip with, so `20260719123012_scheduled_rides_coords_and_status_enum` adds `pickupLat/Lng`, `dropLat/Lng`, `paymentMethod`, and converts `status` to a proper `ScheduledRideStatus` enum — same `migrate diff` + hand-stripped-GiST-drops + `migrate resolve --applied` workaround as every prior migration in this project, verified via a from-scratch `migrate deploy`, 10/10 migrations, all 5 GiST indexes intact). Key design decision: the actual `Trip` row is NOT created at scheduling time, only at dispatch — `ScheduledRide` holds everything needed (coords, address, vehicle type, payment method) until then, specifically so booking a ride for next week never counts as the rider's one "active trip" and blocks them from hailing an immediate ride today (the schema's pre-existing `scheduled` `TripStatus` + its state-machine transition were tempting but would have caused exactly that collision, so they're left unused). `jobs/scheduledRides.ts` schedules two BullMQ delayed jobs per ride (re-scheduled on every edit): a reminder push at T-`scheduled_reminder_minutes` and, at T-`scheduled_dispatch_lead_minutes`, a combined reminder+dispatch job that calls the exact same `dispatch/service.ts:requestTrip` path an immediate request goes through, then links the resulting trip back (`status: dispatched`). A rider with a genuinely conflicting active trip at dispatch time gets a clear failure push instead of the ride silently vanishing (`tripId` stays null, `status` still flips to `dispatched` meaning "handed off"). Cancel before dispatch just drops the pending BullMQ jobs; after dispatch it routes through the normal `cancelTrip` so cancellation-fee rules still apply. `GET /drivers/scheduled` covers the driver-app-list requirement; rider/driver UI (datetime picker, list rendering) deferred, same cadence. One BullMQ gotcha hit and fixed: custom job IDs can't contain `:` (unlike ledger idempotency keys, which use `:` freely) — job IDs are hyphen-joined instead. Phase 3.4 — Favorite locations + share tracking done: favorite locations needed zero new backend work (full CRUD already shipped in Phase 0.4's users module — `/users/me/favorites`, tested since then), so this phase was entirely share tracking. `POST /trips/:id/share` (participant-only) mints an opaque token stored in Redis (`shared/shareTokens.ts` — token → tripId, TTL from `system_config`'s `share_link_ttl_hours`; deliberately not a Trip column since it's a short-lived derived value, no migration needed). `GET /trips/:id/track?token=...` is mounted outside the trips router's `requireAuth` gate and returns a minimal read-only view — status, pickup/drop coords+address, driver name, vehicle plate/model/color — with no OTP, payment method, fare, or phone numbers. The `/public` Socket.IO namespace, registered but inert since Phase 0.5, now authenticates its handshake against `{tripId, token}` instead of a JWT and auto-joins the same `trip:{id}` room the authenticated `/app` namespace uses; `realtime/index.ts:emitToTrip` now broadcasts to both namespaces, so a logged-out tracker gets the same live `trip:status`/`trip:driver_location` events a participant sees, for as long as their token is valid. Live E2E-verified the full relay end to end: a real driver socket's `driver:location` ping, through the existing `relayAndRecordTripLocation` path, arrived at a separately-connected `/public` client authenticated only with the share token. Minimal public React tracking page and rider-app favorites-picker wiring both deferred, same backend-then-app cadence as the rest of Phase 3. Phase 3.5 — In-trip chat + calling done (backend; no migration, ChatMessage already existed from Phase 0's schema and `chat:send`/`chat:message` were already placeholder-documented in socket-events.md from Phase 0.5): `chat:send` over the `/app` socket persists a `ChatMessage` row, relays it live to the trip room as `chat:message` (both roles can send — unlike the driver-only handlers it sits next to in `realtime/index.ts`, via the same dynamic-import pattern already used there to dodge a `chat/service.ts` <-> `realtime/index.ts` import cycle), and separately notifies the other participant through the existing `notifications/service.ts:sendToUser` pipeline (DB row + socket + best-effort FCM) — that FCM push is what actually reaches a backgrounded app, satisfying the plan's "FCM when backgrounded" requirement without any new push-only code path. `POST/GET /chat/:tripId/messages` is a REST fallback/list alongside the socket path, and `POST /chat/:tripId/read` marks the counterparty's messages read for an unread-badge count. Call buttons need zero backend work for v1 (plain `tel:` link, entirely app-side); Twilio masked-proxy calling stays a noted v2 upgrade, not built. Live E2E-verified the trickiest part end to end over real sockets: rider and driver both connect, the driver accepts via the genuine `trip:driver_response` socket event (not an internal function call — an earlier attempt using a freshly-connected socket after accepting via a separate script correctly failed, since `joinTripRoom` only moves currently-connected sockets into the trip room at the moment of acceptance, confirming this is a real, pre-existing characteristic of the realtime layer rather than a bug), then the rider's `chat:send` arrives at the driver as `chat:message`. Chat UI and unread badges in both apps deferred, same cadence as the rest of Phase 3. Phase 3.6 — Notifications center done (backend; no migration — the in-app list endpoint, `GET /users/me/notifications`, already existed from Phase 0.5, so this was entirely the admin broadcast side): `POST /admin/broadcasts` resolves a segment to concrete userIds — `all_riders`/`all_drivers` directly off `User.role`; `zone` to currently-*online* drivers (any vehicle type) whose live Redis GEO position falls inside the zone's polygon via `ST_Contains` (riders have no persistent live position, so `zone` only ever targets drivers) — then hands the actual sending off to a new BullMQ job (`jobs/broadcasts.ts`) rather than doing it inline, since a segment can be thousands of recipients and the per-recipient FCM call inside the existing `sendToUser` shouldn't block the admin's HTTP response; one recipient's delivery failure never aborts the rest of the batch. Every broadcast is audit-logged with the resolved recipient count, same `AuditLog` trail as the payout/owe admin actions from Phase 2.5. Live E2E-verified both an `all_riders` broadcast and a zone-scoped one (a real online driver positioned inside the seeded zone via its live GEO state got notified; the broadcast endpoint correctly rejected a non-admin caller). In-app notification list *screens* (as opposed to the list endpoint itself) deferred, same backend-then-app cadence as the rest of Phase 3. **Phase 3 — Growth Features complete (3.1–3.6).** Phase 4.1 — Web scaffold + RBAC done: apps/web's login/routing was pure placeholder before this (Phase 0.1 scaffold only — LoginPage didn't call the API, AdminLayout/AdminDashboard were static markup, no route guards). Wired `POST /auth/staff/login` for real, with the session (user + access/refresh tokens) persisted to localStorage; `apiClient.ts` gained a response interceptor that retries a 401 once after a token refresh and drops the session to `/login` if the refresh itself fails. Routing is now genuinely role-gated via a `ProtectedRoute` component reading the top-level `User.role` (admin/staff → `/admin`, fleet_owner → `/company`, dispatcher → `/dispatcher`) — an authenticated user hitting the wrong panel's URL is bounced to their own, and an unauthenticated one to `/login`. Scope decision, disclosed rather than silently punted: "permission-driven" here means role-driven off the existing `User.role` enum, not the finer-grained `StaffRole`/`Permission`/`RolePermission` tables — those exist in the schema but have zero seed data, zero admin UI to assign them, and zero routes that call `requirePermission` (only `requireRole`), so wiring the frontend to consume them now would mean inventing fake permission data Phase 4.2 (which explicitly owns "roles & permissions UI") would just have to replace. `/company` and `/dispatcher` render a shared `PlaceholderPanel` (signed-in-as + sign-out, real auth already works, panel content itself is Phase 4.4); `/admin`'s sidebar links to `/admin/drivers|users|config` now navigate for real, landing on a `ComingSoon` placeholder pending Phase 4.2. Added `@tanstack/react-query` (wired at the app root) and a shared `DataTable` (AntD `Table` wrapper with sensible pagination defaults) per the plan's "shared API client + table/query components" — both currently unconsumed scaffolding, ready for Phase 4.2's first real list screen. Verified in a real headless-Chromium browser via Playwright (installed to the scratchpad only, browsers untouched — this sandbox's pre-installed Chromium binary was reused directly): unauthenticated redirect to `/login` from both `/` and `/company`, wrong-password error staying on `/login`, admin login landing on `/admin` with real dashboard markup, sidebar nav to the `ComingSoon` placeholder, session surviving a hard reload, logout clearing the session and re-blocking `/admin`, and both a fleet_owner and a dispatcher login landing on their own placeholder panel with cross-panel access correctly denied — 10/10 checks passed. No backend changes this sub-phase. Phase 4.2 — Admin core done (backend + frontend, zero schema migrations — every table/column needed, `DriverProfile.documents`, `SystemConfig`, `StaffRole`/`Permission`/`RolePermission`, `User.staffRoleId`, already existed unused from Phase 0's schema): six areas. `admin/service.ts:getDashboardStats` buckets today's trips by `Trip.createdAt`, revenue by `Trip.paidAt`+`paymentStatus:"paid"`, active drivers by `DriverProfile.online`, completion rate as `completedToday/tripsToday`. `getDriverDocuments` reuses `shared/storage.ts:getSignedObjectUrl` for the document viewer — SigV4 presigning needs no network round-trip, so it's fully testable/E2E-verifiable even without MinIO running (only the actual image bytes are the disclosed-unverified part, same category as the GOOGLE_MAPS_API_KEY/STRIPE_SECRET_KEY gaps elsewhere). `users/service.ts` gained admin CRUD (`adminListUsers`, `createStaffUser`, `adminUpdateUser`, `setUserStatus`) — staff/admin/fleet_owner/dispatcher accounts need a real phone number as input since `User.phone` is unique+required with no default, rather than migrating it nullable; every create/role-assign/status-change is audit-logged. `vehicles/service.ts` gained `listAllVehicleTypes`/`createVehicleType`/`updateVehicleType`/`listVehicles`/`updateVehicle`, admin-only and mounted under `/admin` (not `/vehicles`, which stays the public read-only list) per this codebase's established pattern of admin-facing CRUD living in admin/routes.ts importing the owning module's service. `shared/config.ts` gained `listAllConfig`/`setConfigValue` — the editor only edits keys the app already reads by name (404s rather than inventing a new tunable nothing consumes) and deletes that key's 30s in-memory cache entry so an admin's edit is visible immediately, not after the TTL. Roles & permissions (`listPermissions`/`listRoles`/`createRole`/`updateRole`/`deleteRole`) is a deliberately scoped CRUD-only pass: it populates `StaffRole`/`Permission`/`RolePermission` (confirmed zero pre-existing call sites of `requirePermission` anywhere in the codebase before this phase) but does not wire `requirePermission()` into any route gate yet — inconsistent enforcement across admin routes, or accidentally locking out an admin-role user with no assigned `StaffRole`, was judged worse than leaving enforcement role-based (`requireRole("admin","staff")`) for one more phase; `deleteRole` blocks while a role is still assigned to a user. `db/seed.ts` now also upserts 8 permissions and a "Super Admin" `StaffRole` with all of them attached, assigned to the seeded admin. All six areas covered by new `tests/admin-*.test.ts` files (24 new tests, full suite 252/252 passing 3x clean) and live E2E-verified over real HTTP against a running dev server (temp password set/cleared on the seeded admin). Frontend: `AdminDashboard` now shows real stat cards plus a Recharts bar chart (trips today vs. active drivers — revenue and completion rate stay as `Statistic` cards since their units don't share an axis); new `DriversPage` (verification queue with approve/reject and a document-viewer modal), `UsersPage` (filters, create-staff modal, edit, suspend/reactivate), `VehiclesPage` (vehicle-type and vehicle tabs), `ConfigPage` (inline edit, values round-tripped as raw JSON so any config shape works), `RolesPage` (checkbox permission assignment) — all wired into `App.tsx`'s routes and `AdminLayout`'s sidebar, replacing the `ComingSoon` placeholders (which is now unused and deleted) for Drivers/Users/Config and adding Vehicles/Roles. Verified in a real headless-Chromium browser via Playwright (same scratchpad-install pattern as 4.1): dashboard stat cards + chart render, driver document-viewer modal displays both uploaded documents (broken image icon expected — same disclosed MinIO-not-running gap as the backend note above), full create/search/suspend flow on Users, vehicle-type creation, inline config edit, and role creation with a permission checked — 8/8 checks passed, all E2E artifacts cleaned up afterward. Phase 4.3 — Live ops done (backend + frontend, zero schema migrations): the realtime map is served two ways — a REST snapshot on page load (`GET /admin/live/drivers`, `GET /admin/live/trips`) plus live deltas over the `/admin` Socket.IO namespace, registered since Phase 0.5 but inert until now. `admin:trip_status` is emitted from the single choke point `trips/service.ts:transitionTrip` (CLAUDE.md rule 2 — the only place `Trip.status` ever changes), so every lifecycle event reaches the admin map for free without touching each call site; `admin:trip_new` fires alongside trip creation; `admin:driver_location`/`admin:driver_status` mirror the existing `driver:location`/`driver:availability` socket handlers (and the offline-grace BullMQ job on a disconnect timeout) without replacing the rider-facing events. `listOnlineDrivers` only returns drivers still in the Redis GEO *dispatch* pool — a driver mid-trip is removed from that pool on accept (pre-existing, unrelated behavior), so an on-trip driver's position instead comes through `listActiveTripsForMap`'s `driverLocation` field off the same live state hash — both layers stay consistent. Trip detail's route replay (`GET /admin/trips/:id`) reuses the exact `trip_locations` query `completeTrip` already ran for distance calculation, exposed as `getTripRoute` (flushes the in-process location buffer first). Demand heat map is every trip's pickup point, optionally windowed by `Trip.createdAt`; supply heat map is live GEO-pool positions — deliberately no time filter on supply, disclosed rather than silently punted: there's no historical driver-position store, only the live index, so it's always a "right now" snapshot. 8 new backend tests including a real end-to-end Socket.IO test (`tests/admin-live-socket.test.ts`, same pattern as `tests/realtime-driver.test.ts`); full suite 260/260 passing 3x clean, live E2E-verified over real HTTP with a driver+trip seeded through the actual dispatch/accept/arrive/start path. Frontend: new `/admin/live` page (Leaflet + `react-leaflet` + `react-leaflet-cluster` for driver clustering + `leaflet.heat` for both heat layers, OpenStreetMap tiles — no API key needed, unlike the Google Maps path `shared/maps.ts` already has a fallback for) with toggleable layers and a live `/admin` socket connection (`shared/useAdminSocket.ts`) patching the initial REST snapshot; `/admin/trips/:id` (linked from a live-map trip marker popup) shows full trip detail plus a polyline route replay with play/pause + scrubber. Verified via Playwright in a real browser — 6/6 checks passed, including the replay's play button actually advancing the scrubber. Disclosed limitation: this sandbox has no outbound network access to any tile CDN (confirmed via `curl` — `403`/tunnel failures on both `tile.openstreetmap.org` and `unpkg.com`), so the Leaflet base map renders as blank gray tiles here; everything not dependent on external imagery — markers, popups, clustering, heat layers, live socket updates, route replay — was verified working, and the base map would render normally wherever outbound HTTPS is unrestricted. Phase 4.4 — Dispatcher/company panel done (backend + frontend, zero schema migrations): seven backend areas plus a two-tier RBAC split. `admin/routes.ts`'s single router-wide `requireRole("admin","staff")` gate became per-route — `STAFF_ONLY` (identity/compliance/config/RBAC) stays admin/staff-exclusive; `OPS_ROLES` (dashboard, live map, trip management, payouts, owe, broadcasts, statements) now also admits `dispatcher`, matching the plan's explicit feature list. `fleet_owner` gets no router-wide access at all — just one new `GET /admin/my-fleet` endpoint scoped to `Vehicle.fleetOwnerId`/`DriverProfile.fleetOwnerId` (present since Phase 0, unused until now): a deliberate scope decision that the company panel is a read-only "my fleet" view, not a parallel operational surface — platform financial operations stay dispatcher/admin-only. Manual booking reuses `dispatch/service.ts:requestTrip` with a new optional `driverId`: when set, `assignDriverToTrip` offers the trip to exactly that one driver (still acquiring their dispatch lock) and immediately accepts on their behalf, reusing the real offer/accept bookkeeping rather than duplicating it — and checks live GEO-pool membership, not just `DriverProfile.online`, catching a real bug (a driver mid-trip is still "online" in the DB) via a test written to expect the opposite, fixed before merge. `reassignTripDriver` shares that pool check and patches `driverId`/`vehicleId` directly rather than through `transitionTrip`, since it never changes `Trip.status` (rule 2 only governs status transitions). `cancelTripAsAdmin` stays migration-free — `cancelledBy` stays `null` rather than adding an enum value for one more reporting nicety, with the new `AuditLog` entry as the record of who/why — and building it surfaced a real pre-existing gap in the original `cancelTrip`: a driver on a cancelled trip was never released back to the live GEO pool, silently stuck "phantom busy"; both cancel paths now share a `releaseDriverBackToPool` fix. Reports (`reports/service.ts` was a stub) got a `shared/csv.ts:toCsv` helper generalizing the CSV pattern `jobs/weeklyStatements.ts` already established rather than a third hand-rolled copy; financial (daily gross fare/commission/net driver-earnings) and operations (status breakdown, completion rate, fare/distance averages) reports both stream `format=csv`. The zone polygon editor (`zones/routes.ts` was also a stub) follows the existing raw-geometry precedent exactly — `ST_GeomFromText`/`ST_AsGeoJSON` — and needed zero fare-engine changes since `fareOverrides` was already live-wired into `fares/service.ts` since Phase 0/1; a test creates a zone with an overridden `baseFare` and confirms a fare estimate inside it jumps immediately, satisfying the Phase 4 acceptance criterion directly. 40 new backend tests across 6 files (the RBAC split itself was verified live over real HTTP as three logged-in roles rather than a new test file, since this codebase has never done HTTP-level route testing — only service-layer units); full suite 286/286 passing. Live E2E-verified over real HTTP: a dispatcher booking a ride with a chosen driver (immediately `accepted`), reassigning it, admin-cancelling it, and a zone-created fare override showing up in a live fare estimate. Frontend: a full dispatcher panel (`DispatcherLayout` + 8 new pages, including a CSV-download helper using a blob URL since a plain `<a href>` can't carry the auth header, and a zone editor combining `react-leaflet` with `leaflet-draw` wired directly via `useMap()` — no maintained React wrapper exists for v4) reusing Phase 4.3's `LiveOpsPage`/`TripDetailPage` outright via a new `basePath` prop rather than duplicating ~250 lines of map code; a lightweight company panel (`CompanyLayout` + one `CompanyFleetPage`). Verified via Playwright across both panels and three roles — 15/15 checks passed. Phase 4.5 — Rider & driver web panels (thin) done: ride history, a trip receipt view, wallet/transactions, and profile, reusing existing endpoints wherever the plan's "minimal scope" allowed. Only one backend addition — `GET /trips` (`listMyTrips`, cursor-paginated, same convention as `wallet/service.ts:listTransactions`) — everything else (`GET /trips/:id` for receipts, `GET /wallet/balance`+`/transactions`, `GET`/`PATCH /users/me`) already worked for any role untouched. The real gap was frontend auth: apps/web had no way for a rider or driver to log in at all — `shared/auth.ts`'s `StaffRole`/`StaffUser` only modeled the four staff-ish roles via email+password. Widened to a `UserRole` union including `rider`/`driver` (nullable `phone` alongside the existing nullable `email`), `AuthProvider` gained `requestOtp`/`verifyOtp` against the existing `POST /auth/otp/request`/`verify`, and `LoginPage` became tabbed — original email+password under "Staff", new phone+OTP with a rider/driver segmented control under "Rider / Driver". One real bug caught by E2E testing before it shipped: the first cut used antd's `Input.OTP` with a hardcoded `length={6}`, but `otp_length` is itself a `system_config` value (default 4, CLAUDE.md rule 10) — `Input.OTP` only fires `onChange` once every box is filled, so a 4-digit dev OTP could never fill 6 boxes and the verify button stayed permanently disabled; fixed by swapping in a plain numeric `Input` (4-8 digits, matching the backend's own OTP schema bounds) that doesn't assume a fixed length. Both panels mount twice — `/rider/*` and `/driver/*` — over one shared `panels/account/` component set (`AccountLayout` parameterized by `title`+`basePath`, plus `RideHistoryPage`/`TripReceiptPage`/`WalletPage`/`ProfilePage`), the same `basePath`-prop reuse pattern Phase 4.4 used for `LiveOpsPage`, so the four pages are written once and stay role-agnostic. Verified via Playwright in a real browser against seeded rider/driver accounts with a real completed cash trip: OTP login landing each role on its own panel, ride history listing the trip, the receipt rendering the full fare breakdown, wallet showing the settled transaction, profile showing the phone number, a rider hitting `/admin` correctly bounced back to `/rider`, and logout clearing the session — 13/13 checks passed, plus a regression check that the staff email+password tab still renders and rejects bad credentials correctly. Full backend suite 289/289 passing 3x clean (one new test file, `my-trips.test.ts`). **Phase 4 — Panels & Analytics complete (4.1–4.5).** Next: Phase 5 — Hardening & Launch.**

## Flutter SDK
No Flutter SDK ships with this environment by default. It was cloned into `/tmp/flutter-sdk`
for this session (`git clone https://github.com/flutter/flutter.git -b stable --depth 1`,
then add `bin/` to PATH) — that clone does not persist across sessions/containers, so repeat
it if `flutter`/`dart` aren't found. No Android SDK, no Chrome, and no Linux desktop GTK libs
are installed, so `flutter analyze` and `flutter test` (widget tests, Dart-VM only) are the
available verification tools — there is no way to actually run either app on a device/emulator
here. Both `mobile/rider_app` and `mobile/driver_app` need `.env` copied from `.env.example`
before `flutter test`/`flutter analyze` will pass asset resolution (gitignored, per-developer).
